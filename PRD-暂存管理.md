# 暂存管理 PRD

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 产品名称 | 美仓海外仓系统：暂存管理 |
| 文档版本 | V1.0 |
| 文档状态 | 产品方案与页面交互说明 |
| 编写视角 | 产品经理 |
| 编写日期 | 2026-07-26 |
| 原型基线 | GitHub gh_page 分支的暂存库存页面 |
| 目标读者 | 产品、研发、测试、仓库运营、客服、销售、财务 |

> 本文以当前页面原型为基线，描述目标产品行为、字段和验收标准。当前页面使用前端演示数据，状态流转、文件上传、导出和表单提交尚未接入真实后端。

## 2. 背景与问题

头程货物进入海外仓后，通常需要经历暂存、审批、操作指令处理、放货和出库等多个环节。同一柜货可能按托盘或箱数分批处理，且不同客户的出货方式、目的地和地址字段并不一致。

当前业务需要解决以下问题：

1. 暂存库存、待审批、指令处理和出库记录缺少统一的状态视图。
2. 财务审核状态和仓库指令处理状态需要同时呈现，方便判断是否可以继续流转。
3. 操作指令需要支持费用字段、数量、备注、图片和处理状态的精细化维护。
4. FBA、Walmart、TikTok 和私人地址的放货字段不同，不能使用一套固定的必填规则。
5. 没有操作指令时，批量备注和图片功能不应占据页面空间或产生误操作。
6. 运营需要按客户、柜号、入仓号、托盘、目的地等维度快速筛选和导出数据。

## 3. 产品目标与范围

### 3.1 产品目标

- 建立从暂存库存到放货、出库的统一工作台。
- 用状态页签区分审批、指令处理和出库阶段，并根据阶段切换字段和操作。
- 让操作指令从“文本备注”升级为可维护的费用明细对象。
- 支持不同发货申请类型的字段差异和动态必填校验。
- 保留箱数上限校验，避免放货数量超过当前未发货库存。
- 为后续后端接口、权限和审计提供明确的数据结构和验收口径。

### 3.2 本期范围

- 暂存库存主列表、筛选、状态页签、批量选择和导出。
- 待审批审核抽屉。
- 指令待处理、指令处理中、待出库、已出库等工作视图。
- 放货/运单详情抽屉。
- 操作指令列表、选择、新增、编辑、删除、备注、图片和状态处理。
- FBA、Walmart、TikTok、私人地址四类放货表单。
- 货箱信息展示。

### 3.3 非本期范围

- WMS、承运商和财务系统的真实接口开发。
- 真实文件存储、图片审核和病毒扫描。
- 复杂的角色权限后台配置。
- 真实库存扣减、锁箱和事务回滚。
- 轨迹、签收和 POD 的外部数据接入。

## 4. 本期新增与重点优化

| 编号 | 类型 | 功能 | 产品价值 |
| --- | --- | --- | --- |
| F-01 | 新增 | 财务审核、指令审核和指令数量展示 | 让业务人员快速判断记录是否可以进入下一阶段 |
| F-02 | 新增 | 待审批审核抽屉及待审核、已通过、已拒绝、初审页签 | 将审批动作集中到申请详情中 |
| F-03 | 新增 | 指令费用明细字段和指令状态管理 | 支持按费用项跟踪仓库作业 |
| F-04 | 新增 | 指令新增、编辑、删除和指令目录搜索 | 降低手工录入成本，统一费用项 |
| F-05 | 新增 | 单条备注、批量备注、追加/覆盖模式 | 提高批量运营效率 |
| F-06 | 新增 | 指令图片上传、预览、删除和选中下载 | 为仓库作业提供凭证和现场记录 |
| F-07 | 优化 | 无操作指令时隐藏批量备注、下载图片及备注/图片列 | 保持空数据页面简洁，避免无效入口 |
| F-08 | 新增 | FBA、Walmart、TikTok、私人地址动态放货表单 | 适配不同渠道的出货要求 |
| F-09 | 优化 | Walmart/TikTok 的“仓库代码”统一显示为“目的地” | 使用业务人员易理解的字段名称 |
| F-10 | 优化 | Walmart/TikTok 邮编、城市、地址详情改为非必填，新增箱数为必填 | 匹配渠道实际放货资料要求，同时保证数量可控 |
| F-11 | 新增 | 货箱信息展示区 | 支持按箱查看客户数据、系统拣货和回填转单号 |
| F-12 | 新增 | 指令状态草稿保存、放弃和未保存离开提醒 | 避免误丢失批量处理结果 |

## 5. 用户角色与使用场景

| 角色 | 主要任务 |
| --- | --- |
| 仓库运营 | 查看暂存库存、处理仓库指令、上传图片、更新指令状态、执行放货 |
| 跟单/客服 | 查询客户和申请单、维护放货资料、查看审批和出库进度 |
| 财务 | 查看财务审核状态、费用类指令和导出数据 |
| 管理员 | 查看全部数据、执行批量操作和处理异常记录 |

> 权限应由后端接口最终校验。前端隐藏或禁用按钮不能替代接口鉴权。

## 6. 页面与信息架构

当前页面采用单页工作台结构，状态页签和抽屉/弹窗承担不同工作流：

| 页面/区域 | 类型 | 主要用途 |
| --- | --- | --- |
| 暂存库存主列表 | 主页面 | 查看库存、筛选、选择记录、发起放货或更换库位 |
| 待审批 | 主列表状态页 | 查看申请、进入审核申请抽屉 |
| 指令待处理 | 主列表状态页 | 查看待处理指令，批量开始处理 |
| 指令处理中 | 主列表状态页 | 更新单条指令状态，批量完成处理 |
| 指令处理完成，待出库 | 主列表状态页 | 查看已完成指令并执行放货 |
| 已出库 | 主列表状态页 | 查看出库记录和详情 |
| 销毁 | 主列表状态页 | 查看终止记录 |
| 审批拒绝 | 主列表状态页 | 查看被拒绝记录和详情 |
| 审核申请 | 右侧抽屉 | 查看托盘信息、审批申请明细并通过/拒绝 |
| 放货/运单详情 | 右侧抽屉 | 填写放货资料、查看货箱和操作指令 |
| 添加指令 | 右侧抽屉 | 搜索费用目录并选择新增指令 |
| 编辑费用项 | 弹窗 | 修改指令的费用属性 |
| 删除费用项 | 弹窗 | 二次确认删除指令 |
| 批量备注/单条备注 | 弹窗 | 维护指令备注 |

## 7. 主列表设计

### 7.1 状态页签

| 状态 | 含义 | 主要操作 |
| --- | --- | --- |
| 暂存 | 货物已入仓，仍有可操作库存 | 放货、详情、日志、更换库位 |
| 待审批 | 放货申请等待业务或仓库审核 | 审核申请、详情、批量初审 |
| 指令待处理 | 申请已进入仓库指令处理队列 | 开始处理 |
| 指令处理中 | 指令正在执行 | 修改指令状态、处理完成 |
| 指令处理完成，待出库 | 指令全部完成，等待放货 | 放货、详情、日志 |
| 已出库 | 已完成出库 | 详情、应收费用、日志 |
| 销毁 | 终止或销毁记录 | 详情 |
| 审批拒绝 | 审批未通过记录 | 详情 |

页签上的数量必须由后端按当前用户权限和筛选范围返回，不应使用前端写死的数字。

### 7.2 筛选字段

| 字段 | 控件 | 规则 |
| --- | --- | --- |
| 关键字 | 输入框 | 单个精准/模糊搜索，具体匹配字段由后端定义 |
| 客户名称 | 下拉 | 从当前可见数据去重 |
| 申请单号 | 输入框 | 支持模糊查询 |
| 入仓号 | 输入框 | 多个值使用英文或中文分号分隔 |
| 财务审核 | 下拉 | 仅请求类视图显示；值为已审核、部分审核、未审核 |
| 转运方式 | 下拉 | 拆转、直转 |
| FBA 仓库 | 下拉 | 按目的地/仓库代码筛选 |
| 派送方式 | 下拉 | 按承运方式筛选 |
| 库位 | 下拉 | 按当前库位筛选 |
| 柜号 | 输入框 | 支持单个模糊查询 |
| 托盘标签 | 输入框 | 支持单个模糊查询 |
| 入库时间 | 起止日期 | 按入库时间范围筛选 |
| 是否拦截 | 下拉 | 是、否 |

筛选操作包括搜索、重置、展开/收起。按 Enter 应支持提交搜索。

### 7.3 暂存视图字段

| 字段 | 类型/示例 | 说明 |
| --- | --- | --- |
| 客户名称 | 文本 | 货物所属客户 |
| 柜号 | 文本 | 头程柜号 |
| 系统柜号 | 文本 | 系统内部柜号，可为空 |
| 入仓号 | 文本 | 入仓批次或仓库入仓号 |
| 是否拦截 | 枚举 | 是/否 |
| 转运方式 | 枚举 | 拆转/直转 |
| 目的地 | 文本 | FBA 仓库或目标地址标识 |
| 派送方式 | 文本 | Truck-Amazon、Truck-Walmart 等 |
| 托盘标签 | 文本 | 托盘唯一标识 |
| 备注 | 汇总文本 | 指令备注摘要，无备注时显示空状态 |
| 图片 | 汇总文本 | 指令图片数量，无图片时显示空状态 |
| 指令状态 | 状态摘要 | 已处理数/指令总数 |
| 入库时间 | 日期时间 | 货物入仓时间 |
| 重量 | 数字 | 当前库存重量 |
| 体积 | 数字 | 当前库存体积 |
| 总箱数 | 整数 | 母单总箱数 |
| 待审核箱数 | 整数 | 等待审核的箱数 |
| 未发货箱数 | 整数 | 放货数量上限依据 |
| 已发货箱数 | 整数 | 已完成出库的箱数 |
| 操作 | 操作链接 | 放货、详情、日志 |

### 7.4 请求类视图字段

待审批、指令处理、待出库和终止类视图额外展示：

- 客户名称、申请单号、柜号、系统柜号、入仓号；
- 是否拦截、申请类型、Shipment ID、Reference ID；
- 转运方式、派送方式、托盘标签；
- 财务审核（指令处理/出库视图）；
- 指令数量、指令明细；
- 审批视图额外展示备注、图片、指令状态；
- 入库时间、申请箱数、申请箱数总体积、收费托数；
- 根据状态展示审核申请、详情、日志、应收费用等操作。

### 7.5 批量操作与导出

- 列表首列提供全选和单选。
- 暂存视图显示已选择内容汇总：总托盘数、总箱数、总板数、总重量、总体积。
- 待审批视图支持批量初审入口。
- 指令待处理支持批量开始处理。
- 指令处理中支持批量处理完成，但必须先保存指令状态草稿。
- 导出按当前状态生成 CSV，内容与当前列表字段保持一致。
- 有未保存的指令状态修改时，禁止切换状态或导出，需先保存或放弃。

## 8. 状态流转与业务规则

### 8.1 主流程

暂存 → 待审批 → 指令待处理 → 指令处理中 → 指令处理完成，待出库 → 已出库

异常分支：

- 待审批 → 审批拒绝；
- 待出库或履约过程中 → 销毁（需要权限和原因）；
- 任一状态均可查看日志，终止状态只读。

### 8.2 流转规则

1. 待审批记录必须完成审核动作后才能进入后续流程。
2. 指令待处理批量开始处理后，记录进入指令处理中，指令初始状态为待处理。
3. 指令处理中只有全部指令为已处理时，才允许流转至待出库。
4. 没有任何操作指令的记录不得直接标记为“指令处理完成”，需要产品确认是否允许无指令记录走快捷流转。
5. 指令状态修改产生草稿，点击保存后才写入业务数据；放弃则恢复修改前状态。
6. 箱数必须为正整数，且不得大于当前未发货箱数。
7. 详情模式为只读，不能修改放货字段或指令。
8. 所有状态流转都应记录操作人、操作时间、前后状态和原因。

## 9. 审核申请页面

### 9.1 页面结构

审核申请以右侧抽屉展示，分为托盘信息、审核状态页签、申请明细表和底部操作区。

### 9.2 托盘信息字段

| 字段 | 说明 |
| --- | --- |
| 柜号 | 当前申请所属柜号 |
| 托盘标签 | 当前申请所属托盘 |
| 派送方式 | 申请的尾程方式 |
| 客户 | 申请客户 |
| 库位 | 当前托盘库位 |

### 9.3 审核页签与申请明细

页签包括待审核、已通过、已拒绝、初审。申请明细字段包括：

- 选择框、序号；
- 派送方式；
- 目的地（FBA 仓库）；
- 箱数；
- 申请时间；
- 预约发货时间；
- 增值服务；
- 发货申请附件；
- 客户备注信息；
- 操作和日志。

待审核记录提供通过、拒绝、日志操作；已通过、已拒绝和初审页只读展示结果。

## 10. 操作指令管理

### 10.1 指令列表页面

操作指令区域位于放货/运单详情抽屉中，列表字段如下：

| 字段 | 是否必填 | 规则 |
| --- | --- | --- |
| 选择 | 否 | 支持单条选择和全选 |
| 费用代码 | 是 | 来自指令目录；编辑时只读 |
| 费用名称 | 是 | 编辑时只读 |
| 费用类型 | 是 | 仓储费、操作费等 |
| 计费单位 | 是 | 票、箱、KG 等 |
| 计费单价（元） | 是 | 非负数字，支持两位小数 |
| 计费数量 | 是 | 大于 0，支持小数 |
| 币种 | 是 | 人民币、USD 等 |
| 总价（元） | 自动计算 | 计费单价 × 计费数量 |
| 添加时间 | 系统生成 | 记录创建时间 |
| 添加人 | 系统生成 | 记录创建人 |
| 描述 | 否 | 指令说明或原始费用文本 |
| 备注 | 否 | 最多 200 字 |
| 图片 | 否 | 每条指令最多 9 张 |
| 指令状态 | 是 | 待处理、已处理 |
| 操作 | - | 编辑、删除 |

### 10.2 新增指令页面

点击“新增”打开指令选择抽屉：

- 支持按费用代码/名称搜索。
- 支持按费用类型筛选。
- 支持全选当前可用指令。
- 已添加的费用项不可重复选择。
- 列表展示费用代码、名称、类型、计费单位、单价、币种和描述。
- 点击确认后将所选费用项加入当前放货记录，默认数量为 1，状态为待处理。
- 没有选择任何费用项时禁止确认，并提示先选择指令。

### 10.3 编辑与删除

编辑弹窗字段包括费用代码、费用名称、费用类型、计费单位、计费单价、计费数量和币种。费用代码与名称只读，其余字段可编辑；提交时必须通过必填和数值校验。

删除指令必须二次确认，并同步更新当前记录的指令数量、总价和处理进度。

### 10.4 备注功能

- 单条备注：在指令行内点击备注或修改，打开备注弹窗。
- 批量备注：勾选多条指令后使用“批量备注”。
- 批量备注支持追加和覆盖两种模式。
- 追加模式下备注内容不能为空。
- 备注最大长度为 200 字。

### 10.5 图片功能

- 每条指令最多上传 9 张图片。
- 仅允许图片格式，上传后显示缩略图、文件名和数量。
- 支持预览、删除单张图片。
- 勾选包含图片的指令后，可使用“下载图片”批量下载。
- 没有操作指令时，隐藏批量备注、下载图片按钮以及备注/图片列；“新增”按钮继续保留。

## 11. 放货/运单详情页面

### 11.1 公共信息

抽屉顶部只读展示：

- 柜号；
- 当前派送方式；
- 托盘标签；
- 转运方式；
- 未发货箱数。

表单中的“发货申请”用于切换 FBA、Walmart、TikTok 和私人地址模式。切换模式后，只启用当前模式字段，其他模式字段禁用。

### 11.2 FBA 放货字段

| 字段 | 必填 | 规则 |
| --- | --- | --- |
| 发货申请 | 是 | 固定为 FBA |
| 目的地 | 是 | ABE2、ABE3、ABE8、BWI9、FEDEX 等 |
| Shipment ID | 是 | 文本 |
| 派送方式 | 是 | Truck-Amazon、Truck-Walmart、Truck-Other、FEDEX |
| Reference ID | 是 | 文本 |
| 箱数 | 是 | 正整数，不得大于未发货箱数 |
| 预约发货时间 | 是 | 日期 |
| 备注 | 否 | 文本 |
| 附件上传 | 否 | 发货申请附件 |

### 11.3 Walmart/TikTok 放货字段

| 字段 | 必填 | 规则 |
| --- | --- | --- |
| 发货申请 | 是 | Walmart 或 TikTok |
| 目的地 | 是 | 原“仓库代码”字段，统一改为目的地 |
| 箱数 | 是 | 正整数，不得大于未发货箱数 |
| 邮编 | 否 | 渠道允许为空 |
| 收件人 | 否 | 文本 |
| 城市 | 否 | 渠道允许为空 |
| 州 | 否 | 文本 |
| 公司 | 否 | 文本 |
| 地址详情 | 否 | 最多 100 字 |
| 预约发货时间 | 是 | 日期时间 |
| 派送方式 | 是 | Truck-Walmart、Truck-Other、FEDEX、UPS |
| 海外仓备注 | 否 | 最多 500 字 |

重点规则：

1. 邮编、城市、地址详情不参与必填校验。
2. 箱数为新增必填字段，最小值为 1。
3. 箱数不能超过当前记录的未发货箱数。
4. 目的地为必填字段，前端显示名称不再使用“仓库代码”。

### 11.4 私人地址放货字段

| 字段 | 必填 | 规则 |
| --- | --- | --- |
| 派送方式 | 是 | USPS、FEDEX、UPS、Truck-Other |
| 箱数 | 是 | 正整数，不得大于未发货箱数 |
| 预约发货时间 | 是 | 日期 |
| 联系人 | 否 | 文本 |
| 联系电话 | 否 | 电话 |
| 省/州 | 否 | 文本 |
| 城市 | 否 | 文本 |
| 邮编 | 否 | 文本 |
| 联系地址 | 否 | 文本 |
| 备注 | 否 | 文本 |
| 附件上传 | 否 | 私人地址相关附件 |
| 增值服务 | 否 | 贴标、测量尺寸、测量重量、验货、拍照、拍视频、销毁、撕标、重新打板、分拣、开箱 |
| 增值服务备注 | 否 | 文本 |

### 11.5 放货校验与提交

- 表单只校验当前模式已启用的字段。
- 详情模式下所有表单控件只读，底部仅展示关闭。
- 录入箱数后即时清除旧的超限错误。
- 提交前执行浏览器必填校验和业务上限校验。
- 后端提交时必须再次校验库存锁定、箱数、渠道和地址规则。
- 提交成功后应产生放货申请记录、操作日志和状态变更。

## 12. 货箱信息页面

货箱信息位于详情抽屉下方，详情模式展示。字段包括：

| 字段 | 说明 |
| --- | --- |
| 选择 | 当前原型只读展示，后续可支持按箱操作 |
| 货箱号 | FBA 或仓库货箱号 |
| 客户数据 | 客户侧重量、尺寸等数据 |
| 系统拣货（材重/实重） | 系统记录的材重和实重 |
| 承运商 | 当前承运商 |
| 快递标 | 快递标或标签信息 |
| 仓库回填转单号 | 仓库实际回填的转单号 |
| 17 网状态 | 物流网络状态 |
| 状态 | 货箱当前处理状态 |

货箱号是后续库存锁定、分批放货和转单号关联的最小业务单元。

## 13. 核心数据对象

### 13.1 暂存库存记录

| 字段 | 建议类型 | 说明 |
| --- | --- | --- |
| id | Long | 页面记录 ID |
| customer | String | 客户名称 |
| applicationNo | String | 申请单号 |
| container | String | 柜号 |
| system | String | 系统柜号 |
| inbound | String | 入仓号 |
| blocked | Enum | 是否拦截 |
| applicationType | Enum | FBA 仓库、其他地址、Walmart、TikTok、私人地址 |
| transfer | Enum | 拆转、直转 |
| destination | String | 目的地 |
| dispatch | String | 派送方式 |
| pallet | String | 托盘标签 |
| inboundTime | DateTime | 入库时间 |
| weight | Decimal | 重量 |
| volume | Decimal | 体积 |
| boxes | Integer | 总箱数 |
| pending | Integer | 待审核箱数 |
| unsent | Integer | 未发货箱数 |
| sent | Integer | 已发货箱数 |
| financialAudit | Enum | 已审核、部分审核、未审核 |
| instructionAudit | Enum | 已审核、审核中、未审核 |
| status | Enum | 暂存及各流程状态 |

### 13.2 操作指令记录

| 字段 | 建议类型 | 说明 |
| --- | --- | --- |
| instructionId | String | 指令唯一 ID |
| catalogCode | String | 费用目录代码 |
| name | String | 费用名称 |
| type | Enum | 仓储费、操作费 |
| unit | Enum | 票、箱、KG 等 |
| price | Decimal | 计费单价 |
| quantity | Decimal | 计费数量 |
| currency | Enum | 人民币、USD 等 |
| total | Decimal | price × quantity |
| addedAt | DateTime | 添加时间 |
| addedBy | String | 添加人 |
| description | String | 指令描述 |
| remark | String | 指令备注，最多 200 字 |
| images | Array | 图片附件，最多 9 张 |
| status | Enum | 待处理、已处理 |

### 13.3 放货申请记录

放货申请建议至少保存：

- releaseId、inventoryId、applicationNo；
- applicationType；
- destination；
- shipmentId、referenceId；
- dispatch；
- releaseBoxes；
- postalCode、recipient、city、state、company、addressDetail；
- scheduledShippingTime；
- remark、overseasRemark；
- attachments；
- 创建人、创建时间、提交时间、审核状态；
- 前后状态和操作日志。

## 14. 权限、审计与异常处理

### 14.1 权限建议

- 查看权限：按仓库、客户和角色控制。
- 批量初审：仅审批角色和管理员。
- 指令编辑/删除：仓库运营和管理员。
- 指令状态处理：仓库运营。
- 放货：具备出库权限的角色。
- 导出：按角色和数据范围控制。
- 图片下载：仅有指令查看权限的角色。

### 14.2 审计要求

以下动作必须写入日志：

- 状态流转；
- 审批通过/拒绝；
- 指令新增、编辑、删除；
- 备注追加/覆盖；
- 图片上传/删除/下载；
- 放货资料提交和修改；
- 箱数变更；
- 导出。

日志字段至少包括操作人、操作时间、动作、对象 ID、变更前值、变更后值和操作原因。

### 14.3 异常处理

- 箱数超过未发货箱数：阻止提交并提示当前上限。
- 指令处理中存在未完成指令：阻止批量处理完成，并列出申请单号和完成进度。
- 有未保存指令草稿：切换页签、导出或离开页面前二次确认。
- 图片格式不符合要求：拒绝上传并提示支持的格式。
- 重复提交：使用请求幂等号，避免重复生成放货申请。
- 库存已被其他人占用：提交时重新校验并提示刷新。

## 15. 验收标准

### 15.1 主列表

- [ ] 八个状态页签可切换，列表列和操作随状态变化。
- [ ] 所有筛选字段可搜索、重置，入仓号支持分号批量查询。
- [ ] 选择库存记录后，汇总栏正确计算托盘、箱数、重量和体积。
- [ ] 导出文件的字段与当前视图一致。

### 15.2 审批

- [ ] 点击“审核申请”可打开审核抽屉。
- [ ] 审核抽屉显示柜号、托盘、派送方式、客户和库位。
- [ ] 待审核、已通过、已拒绝、初审页签可切换。
- [ ] 通过和拒绝动作产生状态和审计记录。

### 15.3 操作指令

- [ ] 指令列表展示费用代码、名称、类型、单位、单价、数量、币种、总价、添加人、备注、图片和状态。
- [ ] 新增指令支持名称/代码搜索、类型筛选、去重和确认。
- [ ] 编辑时费用代码和名称只读，数值字段通过必填和范围校验。
- [ ] 删除需要二次确认。
- [ ] 单条备注支持新增和修改，批量备注支持追加/覆盖。
- [ ] 图片每条最多 9 张，支持预览、删除和批量下载。
- [ ] 指令为空时不展示批量备注、下载图片和备注/图片列，但保留新增入口。
- [ ] 指令处理完成前，所有指令必须为已处理。

### 15.4 放货

- [ ] FBA、Walmart、TikTok、私人地址切换时只显示并校验当前模式字段。
- [ ] Walmart/TikTok 的目的地为必填，页面不再显示“仓库代码”。
- [ ] Walmart/TikTok 的邮编、城市、地址详情为空时可以提交。
- [ ] Walmart/TikTok 的箱数为空时不能提交。
- [ ] 任一渠道箱数超过未发货箱数时不能提交。
- [ ] 详情模式不可编辑，关闭后不丢失已有数据。

### 15.5 货箱与审计

- [ ] 详情模式可查看货箱号、客户数据、系统拣货、承运商和转单号。
- [ ] 指令、放货、审批和图片操作均可查询日志。
- [ ] 无权限用户不能通过接口绕过前端限制。

## 16. 接口与落地建议

建议拆分以下接口：

| 接口 | 用途 |
| --- | --- |
| GET /staging/inventories | 主列表、页签数量和筛选 |
| GET /staging/inventories/{id} | 暂存详情和货箱 |
| GET /staging/inventories/{id}/instructions | 指令列表 |
| POST /staging/inventories/{id}/instructions | 新增指令 |
| PATCH /staging/instructions/{id} | 编辑指令、备注和状态 |
| DELETE /staging/instructions/{id} | 删除指令 |
| POST /staging/instructions/batch-remark | 批量备注 |
| POST /staging/instructions/{id}/images | 上传图片 |
| DELETE /staging/instruction-images/{id} | 删除图片 |
| POST /staging/release-requests | 提交放货申请 |
| POST /staging/approvals/{id}/decision | 审批通过/拒绝 |
| POST /staging/workflows/batch-transition | 批量状态流转 |
| GET /staging/export | 导出当前视图 |

所有写接口需要支持权限校验、幂等、并发库存校验和审计事件。

## 17. 风险与待确认事项

1. “目的地”字段是自由文本、渠道地址簿还是固定目的地字典，需要业务确认。
2. Walmart/TikTok 的邮编、城市、地址详情目前定义为非必填，是否仍需要格式校验需确认。
3. 放货箱数提交后是否立即扣减未发货箱数，还是进入审批后再扣减，需要确定事务时点。
4. 无操作指令的记录是否允许直接进入待出库，需要明确业务规则。
5. 图片保存期限、单张大小、格式白名单和下载权限需要由仓库与合规共同确认。
6. 审批通过后是否自动进入指令待处理，还是按财务审核和仓库审核分别流转，需要确定状态机。
7. “更换库位”“应收费用”“日志”和“倒计时配置”的后端规则、权限及页面内容仍需补充。
8. 当前原型数据为内存数据，刷新页面会恢复初始状态；正式上线前必须完成接口和持久化。

## 18. 版本记录

| 版本 | 日期 | 说明 |
| --- | --- | --- |
| V1.0 | 2026-07-26 | 基于暂存管理页面整理主列表、审批、指令、放货和货箱信息需求；补充 Walmart/TikTok 字段规则及无指令空状态规则 |
