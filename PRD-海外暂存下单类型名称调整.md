# 海外暂存下单类型名称调整 PRD

## 1. 需求说明

调整海外暂存模块中的下单类型名称：

| 原名称 | 修改后名称 |
| --- | --- |
| 放货 | 发货 |
| 不放货 | 增值服务 |

“销毁”名称保持不变。本次仅修改名称，原有操作流程和业务逻辑保持不变。

## 2. 需要同步修改的位置

### 2.1 下单类型筛选

筛选下拉框中的选项同步修改：

- “放货”改为“发货”。
- “不放货”改为“增值服务”。
- “销毁”保持不变。

### 2.2 下单弹窗

下单类型单选项同步修改：

- “放货”改为“发货”，并继续作为默认选项。
- “不放货”改为“增值服务”。
- “销毁”保持不变。

### 2.3 列表展示

列表“下单类型”列中的标签名称同步修改：

- 原“放货”记录显示为“发货”。
- 原“不放货”记录显示为“增值服务”。

### 2.4 搜索与导出

- 搜索和筛选使用修改后的名称。
- CSV 导出的“下单类型”列使用“发货”“增值服务”“销毁”。

### 2.5 历史数据

- 历史值为“放货”的记录统一显示为“发货”。
- 历史值为“不放货”的记录统一显示为“增值服务”。

## 3. 验收标准

1. 海外暂存相关筛选、弹窗、列表和导出中不再显示“放货”“不放货”。
2. 所有对应位置统一显示“发货”“增值服务”。
3. “销毁”名称和原有业务流程不受影响。
4. 历史记录能够按新名称正常显示和筛选。
